gtest = dependency('gtest', main: true)
test(
    'rotation.h',
    executable(
        'rotation_test.cc',
        sources: ['rotation_test.cc'],
        include_directories: src,
        dependencies: [gtest]
    )
)
